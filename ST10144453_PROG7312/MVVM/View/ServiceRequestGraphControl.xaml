<UserControl x:Class="ST10144453_PROG7312.MVVM.View.ServiceRequestGraphControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ST10144453_PROG7312.MVVM.View"
             mc:Ignorable="d" >
    <Grid Margin="40">
        <Canvas x:Name="GraphCanvas" Background="White" SizeChanged="GraphCanvas_SizeChanged" ClipToBounds="False">
            <!-- Graph will be rendered here -->
        </Canvas>
        
        <Border x:Name="TooltipBorder"
                Visibility="Collapsed"
                Background="White"
                BorderBrush="#CCCCCC"
                BorderThickness="1"
                CornerRadius="4"
                Padding="8"
                MaxWidth="300"
                Panel.ZIndex="100">
            <StackPanel>
                <TextBlock x:Name="TooltipTitle" 
                          FontWeight="Bold" 
                          TextWrapping="Wrap"/>
                <TextBlock x:Name="TooltipDetails" 
                          TextWrapping="Wrap"
                          Margin="0,4,0,0"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
