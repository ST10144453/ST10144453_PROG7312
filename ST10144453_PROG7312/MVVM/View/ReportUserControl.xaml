<UserControl
    x:Class="ST10144453_PROG7312.MVVM.View.ReportUserControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:converter="clr-namespace:ST10144453_PROG7312.FrontendLogic"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewModel="clr-namespace:ST10144453_PROG7312.MVVM.View_Model"
    d:DesignHeight="1080"
    d:DesignWidth="1710"
    Background="Transparent"
    mc:Ignorable="d">

    <Grid Margin="10">
        <!--  Define row and column definitions for layout  -->
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <!--  For ProgressBar  -->
            <RowDefinition Height="Auto" />
            <!--  For Issue Name Label  -->
            <RowDefinition Height="Auto" />
            <!--  For Issue Name TextBox  -->
            <RowDefinition Height="Auto" />
            <!--  For Location Label  -->
            <RowDefinition Height="Auto" />
            <!--  For Location TextBox  -->
            <RowDefinition Height="Auto" />
            <!--  For Category Label  -->
            <RowDefinition Height="Auto" />
            <!--  For Category ComboBox  -->
            <RowDefinition Height="Auto" />
            <!--  For Description Label  -->
            <RowDefinition Height="Auto" />
            <!--  For Description TextBox  -->
            <RowDefinition Height="Auto" />
            <!--  For Media Attachment Button  -->
            <RowDefinition Height="Auto" />
            <!--  For Submit Button  -->
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <!--  Progress Bar  -->
        <ProgressBar
            x:Name="ProgressBar"
            Grid.Row="0"
            Height="20"
            Margin="0,0,0,10"
            Background="LightGray"
            Foreground="DodgerBlue"
            Maximum="100"
            Minimum="0"
            Value="{Binding Progress, Mode=TwoWay}" />

        <!--  Issue Name Label and TextBox  -->
        <Label Grid.Row="1" Content="Issue Name:" />
        <TextBox
            x:Name="IssueNameTextBox"
            Grid.Row="2"
            MinWidth="200"
            MinHeight="30"
            Margin="0,5,0,10"
            Padding="5"
            BorderBrush="Gray"
            BorderThickness="1"
            Text="{Binding IssueName, Mode=TwoWay}" />

        <!--  Location Label and TextBox  -->
        <Label Grid.Row="3" Content="Location:" />
        <TextBox
            x:Name="LocationTextBox"
            Grid.Row="4"
            MinWidth="200"
            MinHeight="30"
            Margin="0,5,0,10"
            Padding="5"
            BorderBrush="Gray"
            BorderThickness="1"
            Text="{Binding Location, Mode=TwoWay}" />

        <!--  Category Label and ComboBox  -->
        <Label Grid.Row="5" Content="Category:" />
        <ComboBox
            x:Name="CategoryComboBox"
            Grid.Row="6"
            MinWidth="200"
            MinHeight="30"
            Margin="0,5,0,10"
            SelectedItem="{Binding SelectedCategory, Mode=TwoWay}">
            <ComboBoxItem Content="Roads and Traffic" />
            <ComboBoxItem Content="Public Utilities" />
            <ComboBoxItem Content="Waste Management" />
            <ComboBoxItem Content="Parks and Recreation" />
            <ComboBoxItem Content="Public Safety" />
            <ComboBoxItem Content="Housing and Buildings" />
            <ComboBoxItem Content="Environmental Concerns" />
            <ComboBoxItem Content="Public Transportation" />
            <ComboBoxItem Content="Health and Sanitation" />
            <ComboBoxItem Content="Community Services" />
            <ComboBoxItem Content="Economic Development" />
            <ComboBoxItem Content="Education and Youth Services" />
        </ComboBox>

        <!--  Description Label and TextBox  -->
        <Label Grid.Row="7" Content="Description:" />
        <TextBox
            x:Name="DescriptionTextBox"
            Grid.Row="8"
            MinWidth="200"
            MinHeight="100"
            Margin="0,5,0,10"
            Padding="5"
            AcceptsReturn="True"
            BorderBrush="Gray"
            BorderThickness="1"
            Text="{Binding Description, Mode=TwoWay}" />

        <!--  Media Attachment Button  -->
        <Button
            x:Name="AttachMediaButton"
            Grid.Row="9"
            MinWidth="200"
            MinHeight="30"
            Margin="0,5,0,10"
            Command="{Binding AttachMediaCommand}"
            Content="Attach Media">
            <Button.Style>
                <Style TargetType="Button">
                    <Setter Property="Background" Value="LightGray" />
                    <Setter Property="Foreground" Value="Black" />
                    <Setter Property="BorderBrush" Value="Gray" />
                    <Setter Property="BorderThickness" Value="1" />
                    <Setter Property="Padding" Value="5" />
                    <Setter Property="FontSize" Value="16" />
                </Style>
            </Button.Style>
        </Button>

        <!--  Submit Button  -->
        <Button
            x:Name="SubmitButton"
            Grid.Row="10"
            MinWidth="200"
            MinHeight="30"
            Margin="0,5,0,10"
            Command="{Binding SubmitCommand}"
            Content="Submit">
            <Button.Style>
                <Style TargetType="Button">
                    <Setter Property="Background" Value="DodgerBlue" />
                    <Setter Property="Foreground" Value="White" />
                    <Setter Property="BorderBrush" Value="DarkBlue" />
                    <Setter Property="BorderThickness" Value="1" />
                    <Setter Property="Padding" Value="5" />
                    <Setter Property="FontSize" Value="16" />
                </Style>
            </Button.Style>
        </Button>
    </Grid>
</UserControl>
